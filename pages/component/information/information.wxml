<view class="main">
    <view class="form-box">
        <form bindsubmit="formSubmit">
            <input value="{{address.name}}" name="name" placeholder="姓名"/>
            <input type="number" name="phone" value="{{address.phone}}" placeholder="联系方式"/>
            <input name="detail" value="{{address.detail}}" placeholder="学号"/>
            <button form-type="submit">保存</button>
        </form>
    </view>
</view>
